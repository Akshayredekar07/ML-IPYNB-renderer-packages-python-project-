[build-system]
requires=['setuptools>=42.0', 'wheel']
build-backend='setuptools.build_meta'

[tools.pytest.ini_options]
testpath=[
    "tests"
]

[tools.mypy]
mypy-path = "src"
igonre_missing_imports = true
